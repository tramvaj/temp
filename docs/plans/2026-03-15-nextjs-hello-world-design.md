# Next.js Hello World — Design

## Goal
Replace `hello.html` with a minimal Next.js 14 App Router project.

## Structure
- `package.json` — next, react, react-dom + dev scripts
- `tsconfig.json` — TypeScript config
- `next.config.js` — minimal config
- `app/layout.tsx` — root layout
- `app/page.tsx` — Hello, World! page

## Notes
- No extra dependencies or tooling
- TypeScript, App Router, no src directory
